/*const { calcularDescuento } = require('./calculator');

test('Monto inválido', () => {
  expect(calcularDescuento(0, 'VIP')).toBe(0);
});

test('Cliente NUEVO', () => {
  expect(calcularDescuento(1000, 'NUEVO')).toBe(50);
});

test('Tipo desconocido', () => {
  expect(calcularDescuento(1000, 'OTRO')).toBe(0);
});*/
